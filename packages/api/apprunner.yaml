version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm install -g pnpm
      - pnpm install --frozen-lockfile
      - pnpm build
run:
  runtime-version: 18
  command: pnpm start
  network:
    port: 3001
    env: PORT
  env:
    - name: NODE_ENV
      value: production



